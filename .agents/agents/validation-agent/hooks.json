{
  "validation-agent-guard": {
    "PreToolUse": [
      {
        "matcher": "invoke_subagent|define_subagent",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os,sys; f='guard.py' if os.path.exists('guard.py') else '.agents/agents/validation-agent/guard.py'; os.execv(sys.executable, [sys.executable, f] + sys.argv[1:])\" --event PreToolUse",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os,sys; f='guard.py' if os.path.exists('guard.py') else '.agents/agents/validation-agent/guard.py'; os.execv(sys.executable, [sys.executable, f] + sys.argv[1:])\" --event Stop",
            "timeout": 15
          }
        ]
      }
    ]
  }
}

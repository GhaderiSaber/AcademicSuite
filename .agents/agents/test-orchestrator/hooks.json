{
  "test-orchestrator-guard": {
    "PreToolUse": [
      {
        "matcher": "run_command|write_to_file|replace_file_content|edit_file|multi_replace_file_content|apply_diff",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os, sys; s = '.agents/agents/test-orchestrator/guard.py' if os.path.exists('.agents/agents/test-orchestrator/guard.py') else 'guard.py'; os.execv(sys.executable, [sys.executable, s] + sys.argv[1:])\" --event PreToolUse",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os, sys; s = '.agents/agents/test-orchestrator/guard.py' if os.path.exists('.agents/agents/test-orchestrator/guard.py') else 'guard.py'; os.execv(sys.executable, [sys.executable, s] + sys.argv[1:])\" --event Stop",
            "timeout": 15
          }
        ]
      }
    ]
  }
}

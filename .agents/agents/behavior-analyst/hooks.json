{
  "behavior-analyst-guard": {
    "PreToolUse": [
      {
        "matcher": "invoke_subagent|define_subagent|write_to_file|replace_file_content|edit_file|run_command",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os,sys; f='guard.py' if os.path.exists('guard.py') else '.agents/agents/behavior-analyst/guard.py'; os.execv(sys.executable, [sys.executable, f] + sys.argv[1:])\" --event PreToolUse",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os,sys; f='guard.py' if os.path.exists('guard.py') else '.agents/agents/behavior-analyst/guard.py'; os.execv(sys.executable, [sys.executable, f] + sys.argv[1:])\" --event Stop",
            "timeout": 15
          }
        ]
      }
    ]
  }
}

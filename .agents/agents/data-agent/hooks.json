{
  "data-agent-guard": {
    "PreToolUse": [
      {
        "matcher": "invoke_subagent|define_subagent|run_command|write_to_file|replace_file_content|edit_file",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os, sys; s = '.agents/agents/data-agent/guard.py' if os.path.exists('.agents/agents/data-agent/guard.py') else 'guard.py'; os.execv(sys.executable, [sys.executable, s] + sys.argv[1:])\" --event PreToolUse",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os, sys; s = '.agents/agents/data-agent/guard.py' if os.path.exists('.agents/agents/data-agent/guard.py') else 'guard.py'; os.execv(sys.executable, [sys.executable, s] + sys.argv[1:])\" --event Stop",
            "timeout": 15
          }
        ]
      }
    ]
  }
}

{
  "test-orchestrator-guard": {
    "PreToolUse": [
      {
        "matcher": "run_command|write_to_file|replace_file_content|edit_file|multi_replace_file_content|apply_diff",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os, sys; s = '.agents/hooks/agents/test_orchestrator_guard.py' if os.path.exists('.agents/hooks/agents/test_orchestrator_guard.py') else 'hooks/agents/test_orchestrator_guard.py'; os.execv(sys.executable, [sys.executable, s] + sys.argv[1:])\" --event PreToolUse",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os, sys; s = '.agents/hooks/agents/test_orchestrator_guard.py' if os.path.exists('.agents/hooks/agents/test_orchestrator_guard.py') else 'hooks/agents/test_orchestrator_guard.py'; os.execv(sys.executable, [sys.executable, s] + sys.argv[1:])\" --event Stop",
            "timeout": 15
          }
        ]
      }
    ]
  }
}

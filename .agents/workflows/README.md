# Deprecated Workflows Directory

> **Deprecation Notice (Antigravity 2026 Standards)**:
> File-based workflows (`.agents/workflows/`) have been deprecated and retired in favor of **Google Antigravity Custom Skills** (`.agents/skills/`) and **Subagents** (`.agents/agents/`).
> 
> All legacy workflow definitions have been archived to `legacy/workflows/`.
> 
> To execute academic workflows, use the appropriate Skill from `.agents/skills/` or invoke the specialized Subagent from `.agents/agents/`.

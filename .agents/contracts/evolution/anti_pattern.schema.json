{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://academic-suite.net/contracts/evolution/anti_pattern.schema.json",
  "title": "AcademicAntiPatternContract",
  "description": "Contract schema for cataloging methodological, statistical, rhetorical, and typographical anti-patterns to prevent recurrence.",
  "type": "object",
  "required": [
    "contract_version",
    "anti_pattern_id",
    "category",
    "defective_pattern",
    "why_defective",
    "observed_symptoms",
    "corrective_remedy",
    "detection_heuristic",
    "target_agent",
    "target_agents",
    "reusable",
    "updated_at"
  ],
  "properties": {
    "contract_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "anti_pattern_id": {
      "type": "string",
      "description": "Unique identifier for this anti-pattern (e.g. AP-STAT-MEDIAN-SPLIT-001)."
    },
    "category": {
      "type": "string",
      "enum": [
        "methodological",
        "statistical",
        "execution",
        "evidence",
        "validation",
        "typography"
      ]
    },
    "defective_pattern": {
      "type": "string",
      "minLength": 5,
      "description": "Description of the defective or discredited approach (e.g. Dichotomizing continuous moderators via median split)."
    },
    "why_defective": {
      "type": "string",
      "minLength": 10,
      "description": "Mathematical or methodological justification for why this pattern is prohibited."
    },
    "observed_symptoms": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string"},
      "description": "Concrete indicators or metrics revealing the anti-pattern (e.g. 50% power loss, inflated Type I error)."
    },
    "corrective_remedy": {
      "type": "string",
      "minLength": 5,
      "description": "The approved, compliant replacement technique (e.g. Hayes PROCESS Model 1 continuous moderation)."
    },
    "detection_heuristic": {
      "type": "object",
      "required": ["trigger_rule"],
      "properties": {
        "trigger_rule": {"type": "string"},
        "regex_patterns": {
          "type": "array",
          "items": {"type": "string"}
        },
        "diagnostic_check_id": {"type": "string"}
      },
      "additionalProperties": false
    },
    "source_pitfall_id": {
      "type": "string",
      "description": "Optional reference to pitfall record in state/pitfalls.jsonl."
    },
    "target_agent": {
      "type": "string",
      "description": "Optional subagent role to which this anti-pattern is strictly targeted."
    },
    "target_agents": {
      "type": "array",
      "items": {"type": "string"},
      "description": "List of subagent roles to which this anti-pattern applies."
    },
    "reusable": {
      "type": "boolean",
      "description": "Whether this anti-pattern should be actively screened across all research projects."
    },
    "is_active_behavior": {
      "type": "boolean",
      "description": "True only after formal promotion. Controls whether this anti-pattern is actively bound to production agent contexts."
    },
    "related_skills": {
      "type": "array",
      "items": {"type": "string"},
      "description": "List of skill directories related to this anti-pattern."
    },
    "graduation_track": {
      "type": "string",
      "enum": ["TRACK_1_IMMEDIATE_GRADUATION", "TRACK_2_CASE_SPECIFIC_LOCAL"],
      "description": "Directive 21 Dual-Track classification."
    },
    "graduation_status": {
      "type": "string",
      "enum": ["PENDING_GRADUATION", "GRADUATED", "NOT_APPLICABLE"],
      "description": "Graduation status under Directive 21."
    },
    "graduated_at": {
      "type": "string",
      "description": "ISO timestamp when this anti-pattern was graduated."
    },
    "graduated_targets": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Files into which this anti-pattern was graduated."
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "additionalProperties": false
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://academic-suite.net/contracts/evolution/improvement_candidate.schema.json",
  "title": "AcademicImprovementCandidateContract",
  "description": "Contract schema for unpromoted behavioral improvement candidates targeting agent system prompts, skill scripts, or heuristic rules.",
  "type": "object",
  "required": [
    "contract_version",
    "candidate_id",
    "target_component",
    "target_type",
    "parent_version",
    "mutation",
    "rationale",
    "expected_improvement",
    "affected_capabilities",
    "author_agent",
    "status",
    "staged_at"
  ],
  "properties": {
    "contract_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "candidate_id": {
      "type": "string",
      "description": "Unique identifier for this improvement candidate (e.g. CAND-2026-CH4-TYPO-001)."
    },
    "target_component": {
      "type": "string",
      "description": "Relative file path of the component targeted for modification (e.g. .agents/agents/academic-writer/agent.md, .agents/skills/apa-reporting/SKILL.md)."
    },
    "target_type": {
      "type": "string",
      "enum": [
        "AGENT_SYSTEM_PROMPT",
        "AGENT_BEHAVIORAL_CONTRACT",
        "SKILL_PROCEDURAL_SPECIFICATION",
        "SKILL_DETERMINISTIC_SCRIPT",
        "HEURISTIC_DECISION_RULE",
        "VALIDATOR_INSPECTION_RULE"
      ]
    },
    "parent_version": {
      "type": "string",
      "description": "Current active version hash or git commit of the targeted component."
    },
    "mutation": {
      "type": "object",
      "required": ["diff_type", "content"],
      "properties": {
        "diff_type": {
          "type": "string",
          "enum": ["UNIFIED_DIFF", "FULL_CONTENT_REPLACEMENT", "PARAMETER_PATCH"]
        },
        "content": {
          "type": "string",
          "minLength": 5,
          "description": "Unified diff text or replacement content representing the exact proposed change."
        },
        "checksum_sha256": {
          "type": "string",
          "pattern": "^[a-fA-F0-9]{64}$"
        }
      },
      "additionalProperties": false
    },
    "rationale": {
      "type": "string",
      "minLength": 10,
      "description": "Attribution and explanation of why this specific mutation resolves the underlying defect."
    },
    "expected_improvement": {
      "type": "object",
      "required": ["target_metric", "baseline_value", "projected_value"],
      "properties": {
        "target_metric": {"type": "string"},
        "baseline_value": {},
        "projected_value": {},
        "qualitative_outcome": {"type": "string"}
      },
      "additionalProperties": false
    },
    "affected_capabilities": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      },
      "description": "List of system capabilities impacted by this candidate mutation."
    },
    "author_agent": {
      "type": "string",
      "description": "Diagnostician or orchestrator agent proposing this candidate (e.g. academic-challenger, statistical-auditor)."
    },
    "status": {
      "type": "string",
      "enum": [
        "STAGED",
        "TESTING",
        "EVALUATION_PASSED",
        "EVALUATION_FAILED",
        "PROMOTED",
        "REJECTED"
      ]
    },
    "staged_at": {
      "type": "string",
      "format": "date-time"
    },
    "associated_pitfall_id": {
      "type": "string",
      "description": "Reference to pitfall in state/pitfalls.jsonl motivating this candidate."
    }
  },
  "additionalProperties": false
}
